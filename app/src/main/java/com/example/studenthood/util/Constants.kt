package com.example.studenthood.util

object Constants {
    const val COLLECTION_NAME_USERS = "users" // constant variable for the  users
    const val COLLECTION_NAME_POSTS = "posts" // constant variable for the  posts
    const val REGEX_STRONG_PASSWORD = "(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[^A-Za-z0-9])(?=.{8,})" //constant to validate the passworod
}